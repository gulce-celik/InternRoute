# Demo login (screenshots / local demos)

| | |
|--|--|
| **Email** | `demo@internroute.app` |
| **Password** | `DemoStudent2026!` |

Reseed sample data:

```powershell
cd backend
.\.venv\Scripts\activate
python ..\scripts\seed_demo_user.py
```

After seeding, open **Pipeline**, select the Trendyol application (Interview stage), then use **Prep with AI** → Analyze / Cover letter / Mock interview. The seed script also prints deep-link URLs with `application_id`.

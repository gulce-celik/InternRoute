# Core

Uygulama çekirdek modülleri.

## Planlanan Dosyalar

- `config.py` — Pydantic Settings ile ortam değişkenleri
- `security.py` — JWT, password hashing
- `dependencies.py` — FastAPI dependency injection (DB session, current user)

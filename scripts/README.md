# Scripts

Geliştirme ve deployment yardımcı scriptleri.

## Planlanan

- `setup.sh` / `setup.ps1` — İlk kurulum (venv, npm install)
- `seed_db.py` — Örnek veri (geliştirme)
- `run_dev.sh` — Backend + frontend paralel başlatma

Sprint 1'de oluşturulacak.
